export const ID_ROOT = 'root';

export const ENDPOINT_MESSAGE = '/message';

export const KEY_TEXT = (id: number): string => `text-${id}`;

export const MAX_FILE_SIZE = 100000000;

export const METHOD_POST = 'POST';

export const TIMEOUT_MESSAGES = 5000;
